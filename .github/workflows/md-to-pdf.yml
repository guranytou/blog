name: md-to-pdf

on:
  push:
    # paths:
    #   - 'content/resume.md'

jobs:
  build:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v2
        with:
          node-version: '14'
    　- run: npm init
      - run: npm i -g md-to-pdf
      - run: md-to-pdf content/resume.md
      - uses: actions/upload-artifact@v1
        with:
          name: resume
          path: ./content/resume.pdf